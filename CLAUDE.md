# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently a blank slate. It contains only a `README.md`
with the project title and no source code, build configuration, or tests yet.

When scaffolding the project, update this file to document:

- **Project overview** — what the project does and its high-level architecture.
- **Build & run** — commands to install dependencies, build, and run the app.
- **Testing** — how to run the test suite and any single-test invocation.
- **Linting & formatting** — commands and tooling used to keep code consistent.
- **Code layout** — the main directories and what lives in each.
- **Conventions** — any project-specific patterns contributors should follow.

## Current Layout

```
.
├── CLAUDE.md   # Guidance for Claude Code (this file)
└── README.md   # Project title
```

## Git Conventions

- The default branch is `main`.
- Use clear, descriptive commit messages.
- Do not create pull requests unless explicitly requested.
